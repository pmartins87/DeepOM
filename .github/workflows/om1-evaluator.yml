name: OM1-OM2 math validation

on:
  push:
    branches: [main]
    paths:
      - "deepom/**"
      - "tests/test_evaluator.py"
      - "tests/test_equity.py"
      - "tools/validate_om1_exhaustive.py"
      - "tools/validate_om2_jackpot_exhaustive.py"
      - ".github/workflows/om1-evaluator.yml"
  pull_request:
    paths:
      - "deepom/**"
      - "tests/test_evaluator.py"
      - "tests/test_equity.py"
      - "tools/validate_om1_exhaustive.py"
      - "tools/validate_om2_jackpot_exhaustive.py"
      - ".github/workflows/om1-evaluator.yml"

jobs:
  math-validation:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Unit, Omaha-rule and equity tests
        run: python -m unittest discover -s tests -v
      - name: Exhaustive 5-card frequency validation
        run: python tools/validate_om1_exhaustive.py
      - name: Exhaustive Omaha Royal Flush probability validation
        run: python tools/validate_om2_jackpot_exhaustive.py
